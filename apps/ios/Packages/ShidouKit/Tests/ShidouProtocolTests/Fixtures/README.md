# Wire fixtures

Captured daemon frames used by round-trip tests. Record new ones with
`swift run shidou-probe <address> <token> --watch <sessionId>` against a live
session, or copy frames from the desktop client's logs.
