// The app icon lives in the repo-root shared assets, same as the SVG icons
// consumed by shidou-icon.tsx, rather than a forked copy under public/.
import appIconUrl from '../../../../assets/app-icon.png'

export const SHIDOU_APP_ICON_URL = appIconUrl
